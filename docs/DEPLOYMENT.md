# Deployment

## Vercel (web)
Deploy `apps/web` with `NEXT_PUBLIC_API_URL` set to API URL.

## Docker (api + infra)
Use `docker compose up -d --build`.

## GitHub Pages demo
Build `demo` and publish `demo/dist` to Pages branch.
